---

# 📰 新闻摘要网站框架修改要点（总结）

## 1. 页面布局

- **单页面流式结构**（类似今日头条）：

  - 顶部 `Header`：包含 logo、分类导航、搜索框。
  - 内容区域：新闻卡片流。
  - 日期分割线：随着滚动吸附在顶部，点击可调出日期选择器。

## 2. 数据流设计

- **新闻数据按日期+分页加载**：

  - 初始：加载当天新闻第 1 页。
  - 如果当天还有下一页 → 继续加载 `page=2`。
  - 如果当天加载完 → 切换到前一天，从 `page=1` 开始加载。

- **日期切换逻辑**：

  - 每个日期块有分割线，随着滚动自动更新顶部日期。
  - 用户点击日期 → 弹出选择器 → 直接跳转到指定日期。

## 3. 无限滚动实现

- 使用 **IntersectionObserver** 监听“加载更多”锚点。
- **预加载优化**：监听点提前（`rootMargin: 0px 0px 400px 0px`），避免滚到底才触发导致空白。
- **防抖 & loading 状态**：

  - `isLoading` 标志位，未完成时不再触发请求。
  - 加载时显示骨架屏或“加载中”提示。

- **结束条件**：当 API 返回 `hasMore: false` 时，停止加载。

## 4. SEO 兼容

- 虽然前端是无限滚动，但 URL 仍要保留分页信息，方便搜索引擎收录：

  - `/2025-09-05?page=1`
  - `/2025-09-05?page=2`

## 5. 交互优化

- **日期吸附效果**：当前日期始终固定在搜索框下方，随滚动自动切换。
- **分类导航置顶**：在 header 中始终可见。
- **骨架屏/占位符**：避免数据加载时出现“空白”。

---

✅ 这样总结下来，你的框架就是：

- **单页面流式体验**
- **按日期+分页的无限滚动加载**
- **SEO 兼容**
- **良好的用户体验（预加载、骨架屏、日期吸附）**

---

LITE:

📰 新闻摘要网站开发任务清单（Cursor 版）

1. 页面框架

创建 单页流式布局

顶部 Header：Logo、搜索框、分类导航（置顶固定）。

内容区域：新闻流（按日期分块）。

日期分割线组件：显示当前日期。

2. 数据加载逻辑

定义新闻 API 请求函数：支持 date + page 参数。

实现 首次加载当天数据（page=1）。

在当前日期数据加载完成后，自动切换到前一天，重新从 page=1 开始加载。

每条新闻渲染为卡片组件，按日期分组。

3. 无限滚动功能

添加 IntersectionObserver 监听“加载更多”锚点。

设置 rootMargin: "0px 0px 400px 0px"，提前 400px 触发加载。

使用 isLoading 状态防止重复请求。

加入 骨架屏/加载提示，提升用户体验。

当 API 返回 hasMore: false → 切换到前一天。

4. 日期吸附逻辑

每个日期块前有一个日期分割线组件。

使用 IntersectionObserver 监听当前“日期分割线”，更新顶部显示日期。

日期显示区固定在搜索框下方，随内容滚动变化。

5. 日期选择功能

在顶部日期区域添加 点击事件 → 弹出日期选择器。

选择日期后 → 直接请求该日期数据，从 page=1 开始加载。

清空旧内容，重新渲染新闻流。

6. SEO 兼容

在前端无限滚动的同时，保证 URL 可更新（/2025-09-05?page=2）。

支持用户刷新时恢复对应的日期 & 页码数据。

7. 分类筛选

Header 中添加分类按钮（政治、经济、科技等）。

点击分类 → 重置新闻流，加载分类下的数据。

分类状态保留在 URL（如 /technology/2025-09-05?page=1）。

✅ 最终效果：

体验上：用户像刷今日头条一样往下拉 → 数据源源不断加载 → 日期自动切换 → 可选日期/分类。

技术上：Cursor 帮你实现无限滚动逻辑、日期切换、SEO URL 保持。

初始页面（顶部状态）

---

| Header（置顶） |
| [Logo] |


---

| [搜索框] |

---

| [日期: 2025-09-05] ← 吸附在搜索框下方 |

---

| 内容卡片 || 内容卡片 || 内容卡片 |

向下滚动后（滚到前一天）

---

| Header（置顶） |
| [Logo] [搜索框] |

---

| [日期: 2025-09-04] ← 自动切换成前一天 |

---

| 内容卡片 || 内容卡片 || 内容卡片 |
